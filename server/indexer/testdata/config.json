{
  "idProperty": "_gml_id",

  "indexes": {
    "名称": {
      "kind": "enum"
    },
    "用途": {
      "kind": "enum"
    },
    "住所": {
      "kind": "enum"
    },
    "建物利用現況_中分類": {
      "kind": "enum"
    },
    "建物利用現況_小分類": {
      "kind": "enum"
    }
  }
}
